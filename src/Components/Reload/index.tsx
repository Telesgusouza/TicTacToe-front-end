import imgReload from '../../assets/reload.svg';
import * as Styled from './style';

export default function Reload() {

    return(
        <>
            <Styled.Rotate alt='rotation right' src={imgReload} />
        </>
    )
}