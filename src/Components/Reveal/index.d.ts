import React from "react";
interface IProps {
    children: React.ReactNode;
    y?: number;
    duration?: number;
    delay?: number;
}
declare function Reveal({ children, y, duration, delay }: IProps): import("react/jsx-runtime").JSX.Element;
export default Reveal;
